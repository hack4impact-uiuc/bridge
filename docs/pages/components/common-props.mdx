export const meta = {type: "components", title: "Common Props"}

import Layout from '../../components/layout.js'
export default Layout({...meta})

Bridge uses [styled-system](https://styled-system.com/) to add sets of styling props to components. 